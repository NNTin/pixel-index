{
  "extends": "../tsconfig.json",
  // run.ts lives outside src/ and drives the real containers, so it needs the
  // API's own types without being part of the API's build.
  "include": ["./run.ts", "../src/**/*"]
}
